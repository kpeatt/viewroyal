# Quick Task: Add a footer to the site that links to the github and the docs site.

## Objective
Add a footer component to the site that links to the GitHub repository and the documentation site.

## Context
- The site is in `apps/web`.
- The main layout is likely in `apps/web/app/root.tsx` or similar.
- The footer should link to the GitHub repository and the docs site (docs.viewroyal.ai).
- It should match the existing styling (e.g., using Tailwind CSS, proper padding, text colors matching the theme).
- This is a quick task, bypassing full milestone planning.

## Requirements
1. Create a `Footer` component.
2. Integrate the `Footer` into the main application layout (`apps/web/app/root.tsx`).
3. Ensure the footer has links to GitHub (e.g. `https://github.com/`) and Docs (`https://docs.viewroyal.ai`).
4. Apply appropriate styling.
