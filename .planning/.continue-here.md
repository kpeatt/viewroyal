---
phase: quick-tasks
task: n/a
total_tasks: n/a
status: completed
last_updated: 2026-02-26T22:14:11.399Z
---

<current_state>
Three quick tasks completed this session. No active phase work — v1.5 milestone just started, Phase 25 (Document Viewer Polish) is next to plan.
</current_state>

<completed_work>

- Quick 9: Fixed TypeError in `update_detector.py` — `.not_()` → `.not_.is_()` (postgrest-py 2.28.0 API change)
- Quick 10: Changed document-viewer.tsx to use `document_section_id` FK for image-to-section mapping instead of naive positional cursor
- Quick 11: Fixed pipeline `match_images_to_sections` — skip remaining same-page images between sections to prevent collage/multi-image pages from bleeding into next section's tags. Re-extracted meeting 3649 images with corrected assignments (16 matched, 7 unmatched→gallery).
- Todo captured: "Extract images before document extraction for Gemini linking" — longer-term fix to pass PyMuPDF images to Gemini during content extraction so it can link them directly
</completed_work>

<remaining_work>

- Verify meeting 3649 images look correct on localhost after re-extraction
- Deploy web app (`pnpm deploy` from `apps/web/`) for quick-10 rendering fix to go live
- Run pipeline update mode for meeting 3649: `uv run python main.py --target 3649 --update-mode --force` (was blocked by quick-9 TypeError, now fixed)
- Start planning Phase 25 (Document Viewer Polish) when ready
</remaining_work>

<decisions_made>

- Used same-page skip heuristic in matcher rather than full Gemini-based image linking (quick fix vs long-term solution)
- Kept both renderer fix (quick-10) and pipeline fix (quick-11) — belt and suspenders
- The first todo about "Connect R2 images to document viewer inline" may now be done/superseded by quick-10 work — check and close if so
</decisions_made>

<blockers>
- None currently
</blockers>

<context>
The image alignment bug was caused by Gemini describing a collage of 3 street photos as 1 image on the title slide (page 3). PyMuPDF extracted 3 separate files. The global cursor consumed 2 extras for later sections, shifting all subsequent matches by 2. The same-page skip fix prevents this class of bug. The longer-term fix (todo) is to pass extracted images to Gemini so it can do the linking itself.
</context>

<next_action>
Start with: Check meeting 3649 on localhost to verify images now match captions. Then deploy web app and run pipeline update mode for meeting 3649.
</next_action>
