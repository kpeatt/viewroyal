---
phase: 37-eval-foundation-quick-wins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/37-01-rag-traces-and-feedback.sql
  - supabase/migrations/37-01-fix-carrried-typo.sql
  - apps/web/app/services/rag.server.ts
  - apps/web/app/routes/api.ask.tsx
  - apps/web/app/routes/api.feedback.tsx
  - apps/web/app/components/search/ai-answer.tsx
autonomous: true
requirements: [SRCH-03, SRCH-04]

must_haves:
  truths:
    - "Every AI answer generates a rag_traces row with query, answer, latency, tools, and source count"
    - "User can click thumbs up or thumbs down on any AI answer"
    - "Thumbs down expands an optional comment field before submitting"
    - "User can toggle between thumbs up and down (not one-shot)"
    - "Feedback is persisted to rag_feedback table with trace_id foreign key"
    - "PostHog analytics continue to fire alongside new DB writes"
  artifacts:
    - path: "supabase/migrations/37-01-rag-traces-and-feedback.sql"
      provides: "rag_traces and rag_feedback tables with RLS policies"
      contains: "CREATE TABLE rag_traces"
    - path: "supabase/migrations/37-01-fix-carrried-typo.sql"
      provides: "Fix CARRRIED typo in motions table"
      contains: "UPDATE motions"
    - path: "apps/web/app/routes/api.feedback.tsx"
      provides: "POST endpoint for feedback submission"
      exports: ["action"]
    - path: "apps/web/app/components/search/ai-answer.tsx"
      provides: "Feedback buttons UI in AI answer footer"
  key_links:
    - from: "apps/web/app/routes/api.ask.tsx"
      to: "supabase rag_traces table"
      via: "getSupabaseAdminClient().from('rag_traces').insert()"
      pattern: "rag_traces.*insert"
    - from: "apps/web/app/components/search/ai-answer.tsx"
      to: "apps/web/app/routes/api.feedback.tsx"
      via: "fetch('/api/feedback')"
      pattern: "api/feedback"
    - from: "apps/web/app/routes/api.ask.tsx"
      to: "apps/web/app/components/search/ai-answer.tsx"
      via: "trace_id SSE event propagation"
      pattern: "trace_id"
---

<objective>
Add RAG trace logging and user feedback infrastructure for AI answers.

Purpose: Establish a measurement baseline for RAG quality so Phase 38 improvements can be quantified. Users get a way to signal answer quality, and every AI answer generates a traceable record.
Output: Two new Supabase tables (rag_traces, rag_feedback), trace ID propagation via SSE stream, feedback buttons in AiAnswer component, POST endpoint for feedback submission, CARRRIED typo fix migration.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-eval-foundation-quick-wins/37-RESEARCH.md
@.planning/phases/37-eval-foundation-quick-wins/37-CONTEXT.md

@apps/web/app/routes/api.ask.tsx
@apps/web/app/components/search/ai-answer.tsx
@apps/web/app/services/rag.server.ts
@apps/web/app/lib/supabase.server.ts

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/app/services/rag.server.ts:
```typescript
export type AgentEvent =
  | { type: "thought"; thought: string }
  | { type: "tool_call"; name: string; args: any }
  | { type: "tool_observation"; name: string; result: any }
  | { type: "final_answer_chunk"; chunk: string }
  | { type: "sources"; sources: any[] }
  | { type: "suggested_followups"; followups: string[] }
  | { type: "done" };
```

From apps/web/app/components/search/ai-answer.tsx:
```typescript
interface AiAnswerProps {
  // (check file for current props -- includes answer, agentSteps, sourceCount, isStreaming, etc.)
}
export function AiAnswer({ ... }: AiAnswerProps)
```

From apps/web/app/routes/api.ask.tsx:
```typescript
function isRateLimited(ip: string): boolean
function getClientIP(request: Request): string
function createStreamingResponse(question: string, context?: string, municipalityName?: string, clientIP?: string)
export async function action({ request }: Route.ActionArgs)
export async function loader({ request }: Route.LoaderArgs)
```

From apps/web/app/lib/supabase.server.ts:
```typescript
export function getSupabaseAdminClient(): SupabaseClient
export function createSupabaseServerClient(request: Request, responseHeaders: Headers): SupabaseClient
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migrations and trace logging in api.ask.tsx</name>
  <files>
    supabase/migrations/37-01-rag-traces-and-feedback.sql,
    supabase/migrations/37-01-fix-carrried-typo.sql,
    apps/web/app/services/rag.server.ts,
    apps/web/app/routes/api.ask.tsx
  </files>
  <action>
1. Create `supabase/migrations/37-01-rag-traces-and-feedback.sql`:
   - CREATE TABLE `rag_traces` with columns: id (uuid PK default gen_random_uuid()), query (text NOT NULL), answer (text), model (text default 'gemini-2.0-flash'), latency_ms (integer), tool_calls (jsonb default '[]'), source_count (integer default 0), sources (jsonb default '[]'), client_ip (text), user_id (uuid references auth.users), posthog_trace_id (text), created_at (timestamptz default now())
   - CREATE TABLE `rag_feedback` with columns: id (uuid PK), trace_id (uuid NOT NULL references rag_traces ON DELETE CASCADE), rating (smallint NOT NULL CHECK rating IN (-1, 1)), comment (text), user_id (uuid references auth.users), client_ip (text), created_at (timestamptz default now())
   - Add unique partial indexes for feedback upsert: one on (trace_id, client_ip) WHERE user_id IS NULL, one on (trace_id, user_id) WHERE user_id IS NOT NULL
   - Add index on rag_traces(created_at DESC) and rag_feedback(trace_id)
   - Enable RLS on both tables. Policies: service_role gets ALL on rag_traces; anon+authenticated get INSERT on rag_feedback; service_role gets SELECT on rag_feedback

2. Create `supabase/migrations/37-01-fix-carrried-typo.sql`:
   - UPDATE motions SET result = 'CARRIED' WHERE result = 'CARRRIED'

3. Add `trace_id` to the `AgentEvent` union type in `rag.server.ts`:
   ```typescript
   | { type: "trace_id"; traceId: string }
   ```

4. Modify `createStreamingResponse` in `api.ask.tsx` to:
   - Generate a traceId using `crypto.randomUUID()` at the start
   - Accumulate trace data during the stream loop (answer text from final_answer_chunks, tool calls, source count from sources event, track start time for latency)
   - After stream completes (after `done` event processing but before controller.close()), insert trace row into Supabase via `getSupabaseAdminClient()` -- fire-and-forget (no await blocking the stream, use `.then()` for error logging)
   - Emit a `{ type: "trace_id", traceId }` SSE event BEFORE the `done` event so the client captures it
   - Keep existing PostHog `captureServerEvent` call alongside the new DB write (dual-write)
   - Use the same `posthog_trace_id` value (already generated as traceId on line ~66) as cross-reference in the trace row
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck</automated>
  </verify>
  <done>
    - rag_traces and rag_feedback migration files exist with correct schema, RLS, and indexes
    - CARRRIED typo fix migration exists
    - AgentEvent type includes trace_id variant
    - api.ask.tsx generates trace ID, accumulates trace data, inserts to rag_traces after stream, emits trace_id event before done
    - PostHog capture still fires (dual-write)
    - typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Feedback API endpoint and AiAnswer feedback buttons</name>
  <files>
    apps/web/app/routes/api.feedback.tsx,
    apps/web/app/components/search/ai-answer.tsx
  </files>
  <action>
1. Create `apps/web/app/routes/api.feedback.tsx`:
   - Export an `action` function handling POST requests only (return 405 for others)
   - Parse JSON body: { traceId, rating, comment }
   - Validate: traceId must be a string, rating must be -1 or 1
   - Reuse `isRateLimited` pattern from api.ask.tsx (or import if extractable) -- use same IP-based approach with `getClientIP`
   - Use `getSupabaseAdminClient()` to upsert into rag_feedback: INSERT with ON CONFLICT handling for the partial unique indexes (try user_id first if available from auth, fall back to client_ip)
   - Return `{ ok: true }` on success, `{ error: string }` on failure with appropriate status codes
   - Handle errors gracefully -- log to console.error, return 500

2. Add feedback buttons to `AiAnswer` component in `ai-answer.tsx`:
   - Add a `traceId` prop to `AiAnswerProps` (string | null)
   - The parent component (search page) must pass the traceId received from the SSE stream
   - Create a `FeedbackButtons` sub-component rendered in the AiAnswer footer area, after the existing confidence indicator and copy button
   - ThumbsUp and ThumbsDown icons from lucide-react (already imported in the project)
   - State: rating (-1 | 1 | null), showComment (boolean), comment (string), submitted (boolean)
   - Thumbs up click: set rating to 1, immediately POST to /api/feedback with { traceId, rating: 1 }, show "Thanks for your feedback" text briefly
   - Thumbs down click: set rating to -1, expand optional comment textarea ("What was wrong?") with a Submit button
   - Submit comment: POST to /api/feedback with { traceId, rating: -1, comment }
   - Toggle behavior: clicking the other thumb sends a new request (upsert handles dedup), updates UI state accordingly
   - If traceId is null, don't render feedback buttons
   - Error handling: silently catch fetch errors (feedback is best-effort, don't disrupt UX)

3. Update the search page component that renders AiAnswer to:
   - Capture the `trace_id` event from the SSE stream and store in state
   - Pass traceId prop to AiAnswer component
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>
    - api.feedback.tsx exists with POST handler, validation, upsert, rate limiting
    - AiAnswer shows thumbs up/down buttons below the answer when traceId is available
    - Thumbs up: highlights button, shows brief confirmation
    - Thumbs down: highlights button, expands optional comment field with submit
    - User can toggle between thumbs up and down
    - Feedback is anonymous; user_id associated if logged in
    - Build passes with no errors
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm typecheck` -- type safety across all modified files
2. `cd apps/web && pnpm build` -- full production build succeeds
3. Manual: Apply migrations to Supabase, ask a question on /search, verify rag_traces row appears with populated fields
4. Manual: Click thumbs up on answer, verify rag_feedback row. Click thumbs down, add comment, verify update
</verification>

<success_criteria>
- Migration files create rag_traces and rag_feedback tables with proper schema and RLS
- Every AI answer generates a trace row with query, answer, latency, tools, sources
- Trace ID propagates from server to client via SSE stream
- Users can rate answers with thumbs up/down, with optional comment on thumbs down
- Feedback persists to database with upsert semantics (toggle support)
- PostHog analytics still fire (dual-write confirmed)
- pnpm build passes
</success_criteria>

<output>
After completion, create `.planning/phases/37-eval-foundation-quick-wins/37-01-SUMMARY.md`
</output>
