---
phase: 37-eval-foundation-quick-wins
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/lib/motion-utils.ts
  - apps/web/app/lib/__tests__/motion-utils.test.ts
  - apps/web/app/components/motion-outcome-badge.tsx
  - apps/web/app/components/meeting-card.tsx
  - apps/web/app/services/meetings.ts
  - apps/web/app/components/meeting/MotionsOverview.tsx
  - apps/web/app/components/meeting/AgendaOverview.tsx
  - apps/web/app/components/search/result-card.tsx
  - apps/web/app/components/home/decisions-feed-section.tsx
  - apps/web/app/components/motion-card.tsx
  - apps/web/app/components/motions-list.tsx
  - apps/web/app/components/meeting-timeline.tsx
  - apps/web/app/components/agenda-card.tsx
  - apps/web/app/components/meeting/MeetingQuickStats.tsx
  - apps/web/app/components/meeting/MeetingTabs.tsx
  - apps/web/app/components/meeting/VideoWithSidebar.tsx
  - apps/web/app/components/meeting/EnhancedVideoScrubber.tsx
  - apps/web/app/components/home/recent-meeting-section.tsx
  - apps/web/app/components/meeting-feed.tsx
  - apps/web/app/routes/person-proposals.tsx
  - apps/web/app/routes/person-votes.tsx
  - apps/web/app/routes/meeting-detail.tsx
autonomous: true
requirements: [MTGX-01, MTGX-02]

must_haves:
  truths:
    - "Motion outcomes display as colored badges everywhere motions appear: green for passed, red for failed, yellow for tabled, gray for withdrawn"
    - "All 11 raw motion result values (including CARRRIED typo) map correctly to 4 display categories"
    - "Meeting list cards show topic indicator chips aggregated from agenda item categories"
    - "Meeting list cards show motion tally (e.g. '8 carried, 1 defeated')"
    - "Meeting list cards show 2-3 lines of truncated summary text (~150 chars)"
  artifacts:
    - path: "apps/web/app/lib/motion-utils.ts"
      provides: "normalizeMotionResult utility and MotionOutcome type"
      exports: ["normalizeMotionResult", "MotionOutcome"]
    - path: "apps/web/app/lib/__tests__/motion-utils.test.ts"
      provides: "Unit tests for motion normalization"
      contains: "normalizeMotionResult"
    - path: "apps/web/app/components/motion-outcome-badge.tsx"
      provides: "Shared MotionOutcomeBadge component"
      exports: ["MotionOutcomeBadge"]
    - path: "apps/web/app/components/meeting-card.tsx"
      provides: "Enhanced meeting card with topic chips, motion tally, summary"
  key_links:
    - from: "apps/web/app/components/motion-outcome-badge.tsx"
      to: "apps/web/app/lib/motion-utils.ts"
      via: "import normalizeMotionResult"
      pattern: "normalizeMotionResult"
    - from: "apps/web/app/components/meeting-card.tsx"
      to: "apps/web/app/services/meetings.ts"
      via: "meeting stats data from loader"
      pattern: "motion_carried_count|topics"
    - from: "apps/web/app/components/meeting-card.tsx"
      to: "apps/web/app/lib/topic-utils.ts"
      via: "import TOPIC_COLORS, TOPIC_ICONS"
      pattern: "TOPIC_COLORS"
---

<objective>
Add motion outcome badges across the app and enhance meeting cards with summary data.

Purpose: Users can scan meeting lists and understand outcomes at a glance -- what topics were discussed, how many motions passed/failed, and a brief summary. Motion outcomes throughout the app show consistent, color-coded badges instead of raw database strings.
Output: normalizeMotionResult utility with tests, MotionOutcomeBadge component replacing 17 inline implementations, enhanced MeetingCard with topic chips + motion tally + summary text, Supabase RPC for meeting stats aggregation.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-eval-foundation-quick-wins/37-RESEARCH.md
@.planning/phases/37-eval-foundation-quick-wins/37-CONTEXT.md

@apps/web/app/components/meeting-card.tsx
@apps/web/app/services/meetings.ts
@apps/web/app/lib/topic-utils.ts
@apps/web/app/components/meeting/MotionsOverview.tsx
@apps/web/app/components/meeting/AgendaOverview.tsx
@apps/web/app/components/ui/badge.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From apps/web/app/lib/topic-utils.ts:
```typescript
export const TOPICS = ["Administration", "Bylaw", "Development", "Environment", "Finance", "General", "Public Safety", "Transportation"] as const;
export type TopicName = (typeof TOPICS)[number];
export const TOPIC_ICONS: Record<TopicName, typeof Building2> = { ... };
export const TOPIC_COLORS: Record<TopicName, string> = { ... };
```

From apps/web/app/services/meetings.ts:
```typescript
export interface GetMeetingsOptions {
  // page, limit, type, year, search, etc.
}
export async function getMeetings(supabase: SupabaseClient, options?: GetMeetingsOptions): Promise<...>
```

From apps/web/app/components/meeting-card.tsx:
```typescript
interface MeetingCardProps {
  meeting: Meeting;
}
export function MeetingCard({ meeting }: MeetingCardProps)
```

From apps/web/app/lib/types.ts:
```typescript
// Meeting type includes: id, title, meeting_date, type, status, summary, video_url, etc.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Motion utilities, badge component, and meeting stats RPC</name>
  <files>
    apps/web/app/lib/motion-utils.ts,
    apps/web/app/lib/__tests__/motion-utils.test.ts,
    apps/web/app/components/motion-outcome-badge.tsx,
    supabase/migrations/37-02-meeting-stats-rpc.sql
  </files>
  <behavior>
    - normalizeMotionResult('CARRIED') returns 'passed'
    - normalizeMotionResult('CARRIED AS AMENDED') returns 'passed'
    - normalizeMotionResult('AMENDED') returns 'passed'
    - normalizeMotionResult('CARRRIED') returns 'passed' (typo safety net)
    - normalizeMotionResult('DEFEATED') returns 'failed'
    - normalizeMotionResult('FAILED') returns 'failed'
    - normalizeMotionResult('FAILED FOR LACK OF A SECONDER') returns 'failed'
    - normalizeMotionResult('FAILED FOR LACK OF SECONDER') returns 'failed'
    - normalizeMotionResult('NOT CARRIED') returns 'failed'
    - normalizeMotionResult('TABLED') returns 'tabled'
    - normalizeMotionResult('WITHDRAWN') returns 'withdrawn'
    - normalizeMotionResult(null) returns null
    - normalizeMotionResult(undefined) returns null
    - normalizeMotionResult('UNKNOWN VALUE') returns null
    - normalizeMotionResult('carried') returns 'passed' (case insensitive)
    - normalizeMotionResult('  CARRIED  ') returns 'passed' (trims whitespace)
  </behavior>
  <action>
1. Create `apps/web/app/lib/motion-utils.ts`:
   - Export type `MotionOutcome = 'passed' | 'failed' | 'tabled' | 'withdrawn'`
   - Export const `RESULT_MAP: Record<string, MotionOutcome>` mapping all 11 raw values (uppercased) to their outcomes
   - Export function `normalizeMotionResult(result: string | null | undefined): MotionOutcome | null` -- uppercases, trims, looks up in RESULT_MAP, returns null for unknown
   - Export const `OUTCOME_STYLES: Record<MotionOutcome, string>` with Tailwind classes: passed = green-100/green-700/green-200, failed = red-100/red-700/red-200, tabled = yellow-100/yellow-700/yellow-200, withdrawn = zinc-100/zinc-600/zinc-200
   - Export const `OUTCOME_LABELS: Record<MotionOutcome, string>` with display labels: Passed, Failed, Tabled, Withdrawn

2. Create `apps/web/app/lib/__tests__/motion-utils.test.ts`:
   - Test all 11 known result values map correctly
   - Test null/undefined/empty string return null
   - Test unknown values return null
   - Test case insensitivity
   - Test whitespace trimming

3. Create `apps/web/app/components/motion-outcome-badge.tsx`:
   - Import Badge from "./ui/badge", cn from "../lib/utils", normalizeMotionResult + types from "../lib/motion-utils"
   - Props: result (string | null | undefined), showVoteCounts? (boolean), yesVotes? (number), noVotes? (number), className? (string)
   - Returns null if normalizeMotionResult returns null
   - Renders Badge variant="outline" with OUTCOME_STYLES colors, OUTCOME_LABELS text, optional vote count display
   - Export as named export `MotionOutcomeBadge`

4. Create `supabase/migrations/37-02-meeting-stats-rpc.sql`:
   - Create RPC function `get_meetings_with_stats()` that returns meeting_id, motion_carried_count, motion_defeated_count, motion_other_count, and topics (text[])
   - Join meetings with motions for counts (CARRIED/CARRIED AS AMENDED/AMENDED/CARRRIED as carried, DEFEATED/FAILED/FAILED FOR LACK OF.../NOT CARRIED as defeated, rest as other)
   - Join meetings with agenda_items and use `normalize_category_to_topic()` for distinct topic array
   - Mark function as STABLE, set search_path to 'public'
  </action>
  <verify>
    <automated>cd apps/web && pnpm exec vitest run app/lib/__tests__/motion-utils.test.ts --reporter=verbose && pnpm typecheck</automated>
  </verify>
  <done>
    - normalizeMotionResult handles all 11 values plus null/undefined/unknown
    - All unit tests pass
    - MotionOutcomeBadge component renders correct colors and labels
    - Meeting stats RPC migration exists
    - typecheck passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Enhanced meeting cards and motion badge sweep</name>
  <files>
    apps/web/app/components/meeting-card.tsx,
    apps/web/app/services/meetings.ts,
    apps/web/app/components/meeting/MotionsOverview.tsx,
    apps/web/app/components/meeting/AgendaOverview.tsx,
    apps/web/app/components/search/result-card.tsx,
    apps/web/app/components/home/decisions-feed-section.tsx,
    apps/web/app/components/motion-card.tsx,
    apps/web/app/components/motions-list.tsx,
    apps/web/app/components/meeting-timeline.tsx,
    apps/web/app/components/agenda-card.tsx,
    apps/web/app/components/meeting/MeetingQuickStats.tsx,
    apps/web/app/components/meeting/MeetingTabs.tsx,
    apps/web/app/components/meeting/VideoWithSidebar.tsx,
    apps/web/app/components/meeting/EnhancedVideoScrubber.tsx,
    apps/web/app/components/home/recent-meeting-section.tsx,
    apps/web/app/components/meeting-feed.tsx,
    apps/web/app/routes/person-proposals.tsx,
    apps/web/app/routes/person-votes.tsx,
    apps/web/app/routes/meeting-detail.tsx
  </files>
  <action>
1. Enhance `MeetingCard` component (`meeting-card.tsx`):
   - Accept additional optional props for meeting stats: `stats?: { motion_carried_count: number, motion_defeated_count: number, motion_other_count: number, topics: string[] }`
   - Add topic indicator chips using TOPIC_COLORS and TOPIC_ICONS from topic-utils.ts: filter to valid TopicName values, exclude "General", limit to 4, render as small pills with icon + label
   - Add motion tally line: e.g. "8 carried, 1 defeated" -- only show if there are motions. Use green text for carried count, red for defeated if > 0
   - Add truncated summary text from meeting.summary: truncate at ~150 chars with ellipsis, render as muted body text below the title
   - Layout: topic chips below title, then summary text, then motion tally at bottom of card

2. Update `getMeetings()` in `meetings.ts`:
   - After fetching meetings, call the `get_meetings_with_stats` RPC to get stats for the same set of meetings
   - Merge stats into meeting results by meeting_id
   - Return the merged data so loaders can pass stats to MeetingCard
   - Update the meeting list routes (likely meetings.tsx or similar) to pass stats prop to MeetingCard

3. Sweep all 17 files that have inline motion result badge logic -- replace with `<MotionOutcomeBadge result={motion.result} />`:
   - Search for patterns like `motion.result === "CARRIED"`, `bg-green-`, `bg-red-` inline conditionals related to motion results
   - Replace each inline badge with `<MotionOutcomeBadge>` import and usage
   - Files to sweep (check each -- some may already be clean or may not need changes):
     * MotionsOverview.tsx -- has carried/defeated filter logic with inline badges
     * AgendaOverview.tsx -- displays motion results inline
     * result-card.tsx -- search result motion badges
     * decisions-feed-section.tsx -- home page decisions
     * motion-card.tsx -- individual motion cards
     * motions-list.tsx -- motion list rendering
     * meeting-timeline.tsx -- timeline motion badges
     * agenda-card.tsx -- agenda card motion display
     * MeetingQuickStats.tsx -- quick stats badges
     * MeetingTabs.tsx -- tab content badges
     * VideoWithSidebar.tsx -- sidebar motion badges
     * EnhancedVideoScrubber.tsx -- scrubber motion markers
     * recent-meeting-section.tsx -- home page recent meetings
     * meeting-feed.tsx -- feed motion badges
     * person-proposals.tsx -- person page proposals
     * person-votes.tsx -- person page votes
     * meeting-detail.tsx -- meeting detail motion badges
   - For each file: import MotionOutcomeBadge, replace inline color logic with the component. Preserve any additional context (vote counts, etc.) by passing props.
   - DO NOT change the filter/grouping logic in MotionsOverview -- only replace the badge rendering
  </action>
  <verify>
    <automated>cd apps/web && pnpm typecheck && pnpm build</automated>
  </verify>
  <done>
    - MeetingCard shows topic chips (up to 4), motion tally, and truncated summary when stats data is available
    - getMeetings returns merged stats data from RPC
    - All 17 files with inline motion badge logic now use MotionOutcomeBadge component
    - No remaining inline CARRIED/DEFEATED color conditionals (except in filter/grouping logic)
    - All 11 motion result values render correct badge color and label
    - pnpm build passes
  </done>
</task>

</tasks>

<verification>
1. `cd apps/web && pnpm exec vitest run app/lib/__tests__/motion-utils.test.ts` -- all normalization tests pass
2. `cd apps/web && pnpm typecheck` -- no type errors
3. `cd apps/web && pnpm build` -- production build succeeds
4. Manual: Browse /meetings, verify topic chips, motion tally, and summary text on meeting cards
5. Manual: View a meeting detail page, verify motion badges show correct colors across MotionsOverview, AgendaOverview, timeline
6. Manual: Check search results with motions -- badges display correctly
</verification>

<success_criteria>
- normalizeMotionResult handles all 11 result values with correct output, plus null/undefined/unknown
- MotionOutcomeBadge renders green/red/yellow/gray badges with Passed/Failed/Tabled/Withdrawn labels
- Meeting list cards show topic indicator chips from agenda item categories
- Meeting list cards show motion tally (carried/defeated counts)
- Meeting list cards show ~150 char truncated summary
- All 17 files with inline motion badge logic replaced with shared MotionOutcomeBadge
- Unit tests pass, typecheck passes, build passes
</success_criteria>

<output>
After completion, create `.planning/phases/37-eval-foundation-quick-wins/37-02-SUMMARY.md`
</output>
