---
phase: 07.1-upgrade-document-extraction-with-docling-and-gemini
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-02-27T21:51:47.201Z
---

<current_state>
Phase 7.1 Plan 03 (backfill + verification). Task 1 (CLI) done. Task 2 (human-verify gate) in progress.

Added `--reextract-images` flag to pipeline. Ran first test (`--limit 2 --concurrency 1`). Two meetings processed: 557 (Public Hearing 2022-05-17) and 3649 (Special Council 2026-02-24). Image matching is working (76/97 images now have `document_section_id`), but there's a **duplicate extracted_documents bug**: meeting 557 has 19 extracted_document rows all for document_id 14 instead of ~1 per document. This needs investigation before running the full backfill.

DB state after test run:
- Total images: 97 (down from ~70k because prepare step wiped all image-bearing docs, only 2 meetings re-extracted so far)
- Images matched to sections: 76/97 (up from 32 pre-reextract)
- Meeting 557 has 19 duplicate extracted_documents for document_id 14 — BUG
</current_state>

<completed_work>

**2026-02-27 session (--reextract-images flag):**
- Added `--reextract-images` argparse flag to `main.py`
- Added `prepare_reextract_images()` method to `pipeline/orchestrator.py`
  - Queries `document_images` to find all `extracted_document_id`s with images
  - Looks up `document_id` -> `meeting_id` chain
  - Deletes `extracted_documents` rows (CASCADE cleans sections + images)
  - Removes meeting IDs from `backfill_progress.json`
- Routing in main.py: calls `prepare_reextract_images()` then `backfill_extracted_documents()`
- Test run: `--reextract-images --limit 2 --concurrency 1` — 2 meetings processed
- Image matching confirmed working (76/97 matched vs 32 before)

**Previous 2026-02-27 session (Gemini image matching):**
- Replaced heuristic image-to-section matching with Gemini-powered numbered image matching
- Added `_create_thumbnail()`, `CONTENT_WITH_IMAGES_PROMPT` to `gemini_extractor.py`
- Modified `extract_content()` to accept optional `images` parameter and send numbered thumbnails
- Added `assign_images_by_number()` to `image_extractor.py`
- Reordered `document_extractor.py`: images extracted BEFORE content, passed to Gemini
- Updated `batch_extractor.py`: JSONL builder includes image thumbnails
- Updated `document-viewer.tsx`: regex handles both `[Image: desc]` and `[Image N: desc]`

**Previous sessions:**
- Plans 07.1-01 and 07.1-02 complete (schema + extractor + pipeline integration)
- Plan 07.1-03 Task 1 complete (backfill CLI)
- First backfill: 711 meetings, 12,528 extracted docs, 40,805 sections
- Document viewer refactored into index + detail routes and deployed

</completed_work>

<remaining_work>

1. **INVESTIGATE: Duplicate extracted_documents bug** — meeting 557 has 19 rows for document_id 14. Check if `extract_and_store_documents()` is creating one `extracted_document` per PDF page instead of per document, or if the cleanup-before-insert isn't running for reextracted meetings.
2. **Fix the bug** and verify with a small test
3. **Run full reextract**: `uv run python main.py --reextract-images --concurrency 5`
4. **Generate embeddings** for sections missing them: `uv run python main.py --embed-only`
5. **Complete Plan 03 human-verify checkpoint**
6. **Phase verification**

</remaining_work>

<decisions_made>

- `--reextract-images` uses the same `backfill_extracted_documents()` path (not batch) after clearing image-bearing docs
- Prepare step queries `document_images` -> `extracted_documents` -> `documents` to find meeting IDs
- Deleting `extracted_documents` cascades to both `document_sections` and `document_images`
- Old R2 image files become orphaned (new extracted_doc_id = new R2 key paths) — R2 storage is cheap, clean up later
- Used `--limit` and `--concurrency` passthrough to `backfill_extracted_documents()` for consistency

</decisions_made>

<blockers>
- Duplicate extracted_documents bug must be fixed before full backfill run
</blockers>

<context>
Key files modified this session:
- `apps/pipeline/main.py` — added `--reextract-images` flag + routing
- `apps/pipeline/pipeline/orchestrator.py` — added `prepare_reextract_images()` method

The duplicate bug is likely in `extract_and_store_documents()` in `pipeline/ingestion/document_extractor.py`. The function may be creating an `extracted_document` row per PDF page or per extraction call rather than one per source document. Or it might be that the cleanup-before-insert logic (commit 6fdcbfb7) only fires on `--force` mode.

DB relationship chain: `documents.meeting_id` -> meetings. `extracted_documents.document_id` -> documents. `document_images.extracted_document_id` -> extracted_documents (CASCADE). `document_sections.extracted_document_id` -> extracted_documents (CASCADE).
</context>

<next_action>
1. Read `pipeline/ingestion/document_extractor.py` — specifically `extract_and_store_documents()` — to understand why 19 extracted_document rows were created for a single document_id
2. Check whether the function creates one extracted_document per agenda item PDF vs per source document, and whether that's expected
3. If bug: fix, clean up meeting 557 dupes, re-test with `--reextract-images --limit 2`
</next_action>
