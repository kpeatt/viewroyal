# Database Schema

`bootstrap.sql` contains the complete database schema for the ViewRoyal.ai platform. Apply it in your Supabase SQL Editor to create all tables, types, indexes, and functions.

Requires the `pgvector` extension for semantic search support.
